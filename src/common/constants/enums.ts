export enum ConfigKey {
  Tasks = 'tasks',
}

export enum GitFileStatus {
  Added = 'A',
  Modified = 'M',
  Deleted = 'D',
  Renamed = 'R',
  Copied = 'C',
}

export enum ToggleLabel {
  On = 'ON',
  Off = 'OFF',
}

export enum VscodeTaskSource {
  Workspace = 'Workspace',
}
