export enum LocationScope {
  Global = 'global',
  Workspace = 'workspace',
}

export enum Position {
  Before = 'before',
  After = 'after',
}

export enum ConfigKey {
  Plugins = 'plugins',
  Prompts = 'prompts',
  Tools = 'tools',
  Scripts = 'scripts',
  Tasks = 'tasks',
}
